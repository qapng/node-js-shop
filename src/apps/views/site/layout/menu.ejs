<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12">
        <nav>
            <div id="menu" class="collapse navbar-collapse">
                <ul>
                <% for (let c of categories) { %>
                    <li class="menu-item"><a href="/category.<%= c._id %>"><%= c.cat_name %></a></li>
                <% } %>
                </ul>
            </div>
        </nav>
    </div>
</div>